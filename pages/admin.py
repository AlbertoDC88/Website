from django.contrib import admin 
#from pages.models import Flight
#
## and register it
#class FlightAdmin(admin.ModelAdmin):
#    model = Flight
#    list_display = ('Origin', 'Destination',)
#    prepopulated_fields = {'slug': ('Origin',)}
#
## and register it
#admin.site.register(Flight, FlightAdmin)